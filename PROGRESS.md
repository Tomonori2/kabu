# 進捗メモ

## 🧾 家計簿アプリ（2026-06-13追加・同じリポジトリの kakeibo.py）

レシートをスマホで撮って読み込む家計簿。株アプリと同じ仕組み（Supabase / Gemini / パスワード）を共用。

### セットアップ（1回だけ）

1. SupabaseのSQL Editorで以下を実行（「Potential issue detected」→ **Run without RLS**）:

```sql
create table if not exists expenses (
    id       bigint generated by default as identity primary key,
    date     date not null,
    store    text,
    category text not null,
    item     text,
    amount   integer not null,
    memo     text
);
```

2. https://share.streamlit.io →「Create app」→ Deploy a public app from GitHub:
   - Repository: `Tomonori2/kabu`
   - Branch: `claude/continuation-from-yesterday-y47arv`
   - **Main file path: `kakeibo.py`** ← ここが株アプリとの違い
   - App URL: 例 `kakeibo-tomonori`
3. Advanced settings → Secrets に**株アプリと同じ4行**を貼る:
   SUPABASE_URL / SUPABASE_KEY / GEMINI_API_KEY / APP_PASSWORD

### 機能

- 🏠 ホーム: 今月の支出・💰残り使える額・予算ゲージ（8割で注意、超過で警告）・カテゴリ別・最近の支出
  - 「ひと月」は**毎月16日リセット**（16日〜翌月15日）。変更は kakeibo.py の RESET_DAY
- ➕ 追加: 📷レシート読み取り（🧾合計1件 or 🛒一品ずつ選択可・HEIC対応）＋手入力
- 📋 履歴: 一覧＋修正・削除
- 📈 分析: 月別支出（16日区切り）・カテゴリ別・よく使うお店
- 🤖 AI: 高坂先生の家計相談（節約アドバイス＋チャット）
- パスワード: なし（Secrets に APP_PASSWORD を入れていないため。入れれば有効になる）

---

## 現在の機能（2026-06-12更新・全機能動作確認済み）

- 🔒 パスワード保護（Secrets の APP_PASSWORD）
- 🏠 ホーム: 今月の損益（月初0円リセット）・通算損益・含み損益・🎯月の目標ゲージ・今年の配当
- ➕ 追加: 取引の記録（日付変更可・証券コードで会社名自動表示・📝メモ）
  ＋📷スクショ自動入力（メモ可）＋💸配当金の記録
- 📋 履歴: 取引一覧（メモ付き・新しい順）＋取引の修正・削除
- 📊 保有: 現在株価・含み損益・📊6か月チャート・👀ウォッチリスト
- 📈 分析: 勝率・累計損益・月別/銘柄別の成績・月別一覧表・🧾年別集計（税金目安）
- 🤖 AI: 高坂先生（Gemini）の銘柄分析＋💼ポートフォリオ診断（メモも考慮）＋チャット追い質問
- 銘柄の合算: 証券コードが同じなら名前が違っても同じ会社として集計
- DB: trades（code/memo列あり）/ settings / watchlist / dividends の4テーブル（RLSなし）
- Secrets: SUPABASE_URL / SUPABASE_KEY / GEMINI_API_KEY / APP_PASSWORD 設定済み

## 運用メモ

- コード更新が反映されないとき: share.streamlit.io → kabu → ⋮ → Reboot
- AIの503エラー: Google側の混雑。自動リトライ済みなので再実行すればOK
- Supabaseダッシュボードはブラウザの翻訳機能オフで使う

## 🎉 アプリ完成・公開済み（2026-06-10）

## ⚠️ メモ・配当機能を使うための作業（1回だけ）

SQL Editor で入力欄を空にしてから、以下を貼り付けて Run
（「Potential issue detected」が出たら **Run without RLS** を選ぶ）:

```sql
alter table trades add column memo text;

create table if not exists dividends (
    id     bigint generated by default as identity primary key,
    date   date not null,
    name   text not null,
    amount integer not null
);
```

## ⚠️ 目標設定・ウォッチリストを使うための作業（1回だけ・実施済み）

SQL Editor（https://supabase.com/dashboard/project/nnsctxvscnkbctylxrxs/sql/new）で
入力欄を空にしてから、以下を貼り付けて Run:

```sql
create table if not exists settings (
    key   text primary key,
    value text not null
);

create table if not exists watchlist (
    id   bigint generated by default as identity primary key,
    code text not null,
    name text not null
);
```

これで「🎯 月の目標」（ホーム）と「👀 ウォッチリスト」（保有タブ）が使えるようになる。

## 🎉 アプリ完成・公開済み（2026-06-10）

**アプリURL: https://kabu-tomonori.streamlit.app/**

スマホのホーム画面に追加しておくと、アプリのようにすぐ開けます。

## 構成

| 役割 | サービス | 場所 |
|---|---|---|
| コード置き場 | GitHub | このリポジトリ（ブランチ: `claude/continuation-from-yesterday-y47arv`） |
| アプリ実行 | Streamlit Community Cloud | https://kabu-tomonori.streamlit.app/ |
| データ保存 | Supabase | https://nnsctxvscnkbctylxrxs.supabase.co の `trades` テーブル |

- `trades` テーブルは Table Editor から手作業で作成（RLSはオフ = UNRESTRICTEDで正常）
- 列: `id`, `created_at`, `date`, `name`, `baibai`, `shares`, `price`
- Streamlit Cloud の Secrets に `SUPABASE_URL` / `SUPABASE_KEY` 設定済み

## 運用メモ

- 無料プランのため、しばらく使わないとアプリがスリープする。開いて1分ほど待てば起動する（故障ではない）
- Supabaseのダッシュボードを見るときは**ブラウザの翻訳機能をオフ**にする（オンだと画面が壊れる）
- データの中身は Supabase の Table Editor からも直接見られる

## ⚠️ 現在株価機能を使うための作業（1回だけ）

`trades` テーブルに `code` 列を追加する必要がある:

1. Table Editor を開く: https://supabase.com/dashboard/project/nnsctxvscnkbctylxrxs/editor
2. 左のリストの `trades` の横の「…」→「**Edit table**」
3. Columns の一番下の「**Add column**」→ Name: `code` ／ Type: `text`
4. 「**Save**」

これをやると、取引追加のときに証券コード（例: トヨタ=7203）を入力でき、
保有状況タブに現在株価と含み損益が表示される。

## 今後のアイデア（やりたくなったら）

- 取引の編集・削除機能（誤入力の修正）
- 損益のグラフ表示
- 現在株価の自動取得と含み損益の表示
- コードを `main` ブランチにマージして整理
